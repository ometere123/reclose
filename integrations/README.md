# integrations/

Optional external integrations (e.g. hosted API/indexer convenience layer). Never a correctness dependency for
protocol/SDK usage (CLAUDE.md Section 32).

No integrations exist yet as of F0.
