# benchmark/

R1 benchmark (minimum 50 scenarios spanning confirmed/rejected/undetermined/adversarial/stale/outage/
remediation/recovery/replay cases - CLAUDE.md Section 37). Each scenario must map to the `TM-*` threat IDs it
exercises (see `docs/security/Threat Status.csv`).

No benchmark scenarios exist yet as of F0 - H1 scope.
