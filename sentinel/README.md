# sentinel/

Optional liveness infrastructure: monitors sources, builds EAPs, submits reports, tracks transactions. Sentinel
never receives target-control authority and never determines truth (CLAUDE.md Section 33).

No Sentinel implementation exists yet as of F0 - C3 scope.
