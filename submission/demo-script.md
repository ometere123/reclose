# Demo script

1. Introduce Reclose as runtime assurance: GenLayer judges evidence; deterministic policy limits consequences.
2. Show the active Studio-dev target, policy key/version/hash, Judge and finite effect list. State that policy activation is verified on-chain.
3. Explain the intended Provider A → Provider B safe-continuity and recovery scenario as the R1 design.
4. Show immutable evidence authority and the synthetic fixture rules, clearly labelling fixtures as demonstrations.
5. Show the saved typed-address `decided` Parent→Child read-only simulation result (`transactionSubmitted: false`; fee is an estimate, not a charge). Explain that it verifies isolated message emission only. The active Kernel/Judge deployment predates the `decided` fix, so signing is paused and no full lifecycle is claimed.
6. State clearly that no incident was submitted on this deployment, so no live containment or recovery outcome is claimed.
7. Show automated checks and the 78-scenario corpus structure, while noting that full H1 metrics and E1 A+B are incomplete.

Do not replace these steps with a live-success narrative unless the E1 evidence checker passes for two independent fresh runs and every displayed transaction/post-state is from the frozen candidate generation.
