{
  "name": "@reclose/protocol-sdk",
  "version": "0.1.0",
  "private": true,
  "description": "Type-only foundation for the frozen Reclose F1 interface boundary (Frontend Contract v1) plus a minimal wrong-network runtime guard. No network calls or C1 business logic at this phase (A0-T1).",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "typescript": "5.9.3"
  }
}
