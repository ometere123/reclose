# Reclose R1 - pinned Python toolchain, exactly matching the G0-verified baseline.
# The [sim] extra does NOT change the genlayer-test version; it installs the package's own pinned
# sandbox/simulator dependencies (including cloudpickle) needed by spawn_sandbox validator tests.
genlayer-py==0.19.0rc2
genlayer-test[sim]==0.30.0rc2
genvm-linter==0.11.1rc2
