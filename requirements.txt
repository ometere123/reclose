# Reclose R1 - pinned Python toolchain, exactly matching the G0-verified baseline.
# See toolchain/versions.lock and toolchain/runner.lock for the full accepted baseline
# and docs/execution/Studio-dev Toolchain & Network Compatibility Record.md for how these
# exact versions were verified. Do not bump without rerunning G0-equivalent conformance.
genlayer-py==0.19.0rc2
genlayer-test==0.30.0rc2
genvm-linter==0.11.1rc2
